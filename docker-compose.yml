version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app:ro
    command: ["uvicorn", "backend.app:app", "--host", "0.0.0.0", "--port", "8000"]
